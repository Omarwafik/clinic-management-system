PORT=3000
# REACT_APP_API_URL is intentionally NOT set here.
# Setting it in .env bakes the value into every production build (including deploys).
# For local development put it in .env.local instead (gitignored):
#   REACT_APP_API_URL=http://localhost:5000/api
